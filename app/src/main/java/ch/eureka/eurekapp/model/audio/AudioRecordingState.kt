package ch.eureka.eurekapp.model.audio

enum class RecordingState {
  PAUSED, // When the recording is paused
  STOPPED, // When the recording is completely stopped
  RUNNING // When the recording is running
}
